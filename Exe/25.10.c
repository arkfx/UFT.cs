#include <stdio.h>
#include <math.h>
int main() {
    /* double num = 16.0;
    double result = sqrt(num);
    printf("The square root of %lf is %lf\n", num, result); */
    printf("Hello World!\n");
    return 0;
}