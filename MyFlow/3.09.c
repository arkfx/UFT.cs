#include <stdio.h>
int main(){
//Some um número indeterminado de inteiros. Se o símbolo digitado entre os números for ‘+’,
//continue somando, se for ‘=’ mostre o resultado da soma. 
int soma = 0, num;
char resp;
do{
    scanf("%d", &num);

    soma = soma + num;







} 
